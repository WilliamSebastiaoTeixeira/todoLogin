# Lista de tarefas - Laravel API + Vue3

## Setup
- Configure o banco no arquivo `.env` 


### API
```bash 
cd api
php artisan migrate
php artisan jwt:secret
php artisan serve
```

### Frontend
```bash
cd web
yarn
yarn run dev
```
